apiVersion: v1
kind: Service
metadata:
  name: ride-service
  namespace: ridebooking
  labels:
    app: ride-service
spec:
  selector:
    app: ride-service
  ports:
    - name: http
      port: 8082
      targetPort: 8082
      protocol: TCP
  type: ClusterIP
